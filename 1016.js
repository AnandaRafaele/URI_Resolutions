var input = require('fs').readFileSync('./stdin', 'utf8');
var lines = input.split('\n');

var car1 = 60
var car2 = 90

var km = parseInt(lines.shif())

var minutos = (km / 2) * 60

console.log(minutos + " minutos")